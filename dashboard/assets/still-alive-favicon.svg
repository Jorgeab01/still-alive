<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <defs>
    <filter id="glow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="4"/>
    </filter>
  </defs>

  <!-- Subtle uptime glow -->
  <circle cx="32" cy="32" r="24" fill="#3fb950" opacity="0.10" filter="url(#glow)"/>

  <!-- Uptime indicator -->
  <circle cx="32" cy="32" r="13" fill="#3fb950"/>
</svg>
